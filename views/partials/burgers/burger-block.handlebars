<li>
	<div class="row">
	<div class="col-8">
		{{burger_name}}
	</div>
	<div class="col-4">
		<button class="change-devour" class="float-right" data-id="{{id}}" data-newdevour="{{devour}}">
			{{#if devour}}SEND{{else}}RETURN{{/if}}
		</button>
	</div>
	</div>
	{{!-- <button class="delete-burger" data-id="{{this.id}}">DELETE!</button> --}}
</li>